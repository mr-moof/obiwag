{
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "provider", "verdict", "scope_complete", "citations", "findings", "limitations"],
  "properties": {
    "schema_version": {"type": "integer", "enum": [1]},
    "provider": {"type": "string", "enum": ["codex", "claude"]},
    "verdict": {"type": "string", "enum": ["pass", "fail", "inconclusive"]},
    "scope_complete": {"type": "boolean"},
    "citations": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "path", "line_start", "line_end"],
        "properties": {
          "id": {"type": "string"},
          "path": {"type": "string"},
          "line_start": {"type": "integer"},
          "line_end": {"type": "integer"}
        }
      }
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "severity", "category", "summary", "evidence_ids", "rationale", "recommendation"],
        "properties": {
          "id": {"type": "string"},
          "severity": {"type": "string", "enum": ["high", "medium", "low"]},
          "category": {"type": "string", "enum": ["invented-api", "missed-edge-case", "test-gap", "security", "regression-risk", "doc-mismatch", "plan-gap", "other"]},
          "summary": {"type": "string"},
          "evidence_ids": {"type": "array", "items": {"type": "string"}},
          "rationale": {"type": "string"},
          "recommendation": {"type": "string"}
        }
      }
    },
    "limitations": {"type": "array", "items": {"type": "string"}}
  }
}
